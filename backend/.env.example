PORT=5000
DATABASE_URL=postgresql://postgres:your_password@db.your_project_ref.supabase.co:5432/postgres
JWT_SECRET=your_jwt_secret_key_here

# SMTP Configuration (Gmail, Mailtrap, Brevo, etc.)
# If left blank, the server will log OTPs and Password Reset links to the console.
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=noreply@shopyzone.com

# Client URL (for CORS)
CLIENT_URL=http://localhost:5173
